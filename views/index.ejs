<% include ./partials/header  %>
<% blogs.forEach(function(blog){ %>
    <div>
           <h2><%= blog.title %> </h2> 
         <img src="<%= blog.image %>" alt="unable to load image">
        <span> <%= blog.created %> </span>  
             <p><%= blog.body %></p>   
    </div>


<% }); %>



<% include ./partials/footer  %>